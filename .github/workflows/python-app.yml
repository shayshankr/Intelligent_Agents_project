name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
        pip install -r Intelligent_Agents_project_files/requirements.txt

    - name: Lint with flake8
      run: |
        flake8 Intelligent_Agents_project_files --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 Intelligent_Agents_project_files --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Run pipeline
      working-directory: Intelligent_Agents_project_files
      run: |
        python main.py
